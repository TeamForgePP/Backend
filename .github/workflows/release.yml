name: release

on:
  push:
    branches: [ main ]

permissions:
  contents: write    # нужно для тегов и релизов

jobs:
  release:
    if: github.event.repository.fork == false
    runs-on: ubuntu-latest

    steps:
      - name: Checkout branch (not detached) with tags
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true
          ref: ${{ github.ref_name }}

      - name: Git identity for release commits
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Setup uv
        uses: astral-sh/setup-uv@v3

      - name: Sync deps
        run: uv sync --all-extras --dev

      - name: semantic-release version
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}   # <-- ВАЖНО: semantic-release читает GH_TOKEN
        run: uv run semantic-release version

      - name: semantic-release publish
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}   # <-- и тут тоже GH_TOKEN
        run: uv run semantic-release publish
